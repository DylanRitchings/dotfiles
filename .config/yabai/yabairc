# Focus windows
# yabai -m config mouse_follows_focus on
# yabai -m config focus_follows_mouse autofocus
yabai -m signal --add event=dock_did_restart action="sudo yabai --load-sa"
sudo yabai --load-sa

yabai -m space 1 --display 0
yabai -m space 2 --display 0
yabai -m space 3 --display 0
yabai -m space 4 --display 0
yabai -m space 5 --display 0
yabai -m space 6 --display 0
yabai -m space 7 --display 0
yabai -m space 8 --display 0
yabai -m space 9 --display 0 --layout float


yabai -m space 10 --display 0
yabai -m space 11 --display 0
yabai -m space 12 --display 0
yabai -m space 13 --display 0
yabai -m space 14 --display 0
yabai -m space 15 --display 0
yabai -m space 16 --display 0
yabai -m space 17 --display 0
yabai -m space 18 --display 0 --layout float


yabai -m config focus_follows_mouse on
yabai -m config window_focus_on_switch on
yabai -m config layout bsp
# yabai -m config top_padding 24

yabai -m config --dispaly 0 external_bar all:00000000:0
yabai -m config --dispaly 1 external_bar all:32:0
yabai -m config --dispaly 2 external_bar all:32:0

# Focus navigation
# yabai -m rule --add app="^.*$" manage=on
# yabai -m config window_gap 0
# yabai -m config window_padding 0
# yabai -m config mouse_modifier none

# # Trigger when a window is created (app is opened)
# yabai -m signal --add event=window_created action="sketchybar --trigger space_windows_change"
#
# # Trigger when a window is moved to a different space
# yabai -m signal --add event=window_resized action="sketchybar --trigger space_windows_change"
#
# # Optional: Trigger when a window is focused (e.g., for dynamic updates)
# yabai -m signal --add event=window_focused action="sketchybar --trigger space_windows_change"

# Focus windows
# yabai -m config focus_modifier.ctrl-left SHIFT  
#  shift. 

# Focus windows
# yabai -m config mouse_follows_focus on
# yabai -m config focus_follows_mouse autofocus

# Focus navigation with Hyper key
# yabai -m key --bind "shift+option+control+h" --action "focus west"
# yabai -m key --bind "shift+option+control+l" --action "focus east"
# yabai -m key --bind "shift+option+control+j" --action "focus south"
# yabai -m key --bind "shift+option+control+k" --action "focus north"
#
# # Move windows
# yabai -m key --bind "shift+option+control+shift+h" --action "move west"
# yabai -m key --bind "shift+option+control+shift+l" --action "move east"
# yabai -m key --bind "shift+option+control+shift+j" --action "move south"
# yabai -m key --bind "shift+option+control+shift+k" --action "move north"
#
# # Switch spaces
# yabai -m key --bind "shift+option+control+1" --action "space --focus 1"
# yabai -m key --bind "shift+option+control+2" --action "space --focus 2"
# yabai -m key --bind "shift+option+control+3" --action "space --focus 3"
# yabai -m key --bind "shift+option+control+4" --action "space --focus 4"
# yabai -m key --bind "shift+option+control+5" --action "space --focus 5"
# yabai -m key --bind "shift+option+control+6" --action "space --focus 6"
# yabai -m key --bind "shift+option+control+7" --action "space --focus 7"
#
# # Move windows to spaces
# yabai -m key --bind "shift+option+control+command+1" --action "window --space 1; space --focus 1"
# yabai -m key --bind "shift+option+control+command+2" --action "window --space 2; space --focus 2"
# yabai -m key --bind "shift+option+control+command+3" --action "window --space 3; space --focus 3"
# yabai -m key --bind "shift+option+control+command+4" --action "window --space 4; space --focus 4"
# yabai -m key --bind "shift+option+control+command+5" --action "window --space 5; space --focus 5"
# yabai -m key --bind "shift+option+control+command+6" --action "window --space 6; space --focus 6"
# yabai -m key --bind "shift+option+control+command+7" --action "window --space 7; space --focus 7"
